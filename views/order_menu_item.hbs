
<link rel="stylesheet" href="/styles.css">


<h1>order_menu_item</h1>
<p>This table is auto generated and the quantity is auto calculated from the order total</p>
[ <a href="/">Index</a> | <a href="/employee"> Employees</a> | <a href="/store">Stores</a> | <a href="/inventory">Inventory</a> | <a href="/menuItem">Menu_Item</a> | <a href="/order">Order</a> | <a href="/customer">Customer</a> ]
{{!-- Create a table --}}
<table id="order_menu_item">

    {{!-- Header section --}}
    <thead>

        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the store_id, location, and franchisee --}}
        {{#each data}}
        <tr data-value={{this.order_id}}>
            <td>{{this.order_id}}</td>
            <td>{{this.menu_item_id}}</td>
            <td>{{this.quantity}}</td>
        {{/each}}
    </tbody>
</table>